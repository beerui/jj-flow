interface:
  display_name: "JJ End"
  short_description: "Close out tasks: commit, push, merge to dev"
  default_prompt: "Use $jj-end to commit, push, merge into dev, push, and return to the working branch."
policy:
  allow_implicit_invocation: true
