interface:
  display_name: "jj-end"
  short_description: "Closeout: sync branches, commit, push, merge into integration"
  default_prompt: "Use $jj-end: fetch, commit if needed, sync work, push work, sync integration, merge, push integration, return."
