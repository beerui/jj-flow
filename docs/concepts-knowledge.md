# 知识库（可选）

跨项目可复用的说明（规范、经验、模式等）。  
默认在用户目录 **`~/.jj-flow/knowledge`**，不塞进 npm 包。

## 和 ralph 的关系

开新的单仓任务时，会检索**当前项目或同一家族**的相关条目（相关才挂，没有就空着）。
首次建库、梳理历史贡献走 `$jj-init`。单轮归档后投喂这一条，可在 ralph 里说「投喂知识库」。**不会**在你点头前写入。

聊天内容 **不会** 自动变成正式规范。

## 相关

[ralph](commands/jj-ralph.md) · [目录](concepts-paths.md)
